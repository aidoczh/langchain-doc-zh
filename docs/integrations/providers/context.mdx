# 背景

[Context](https://context.ai/) 为基于LLM的产品和功能提供用户分析。

## 安装和设置

我们需要安装 `context-python` Python 包：

```bash
pip install context-python
```

## 回调函数

查看[使用示例](/docs/integrations/callbacks/context)。

```python
from langchain.callbacks import ContextCallbackHandler
```