# 天气

[OpenWeatherMap](https://openweathermap.org/) 是一家开源天气服务提供商。

## 安装和设置

```bash
pip install pyowm
```

我们必须设置 `OpenWeatherMap API token`。

## 文档加载器

查看一个[使用示例](/docs/integrations/document_loaders/weather)。

```python
from langchain_community.document_loaders import WeatherDataLoader
```