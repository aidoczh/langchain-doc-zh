# Petals

本页面介绍了如何在LangChain中使用Petals生态系统。

它分为两个部分：安装和设置，以及对特定Petals包装器的引用。

## 安装和设置

- 使用`pip install petals`进行安装

- 获取一个Hugging Face api密钥，并将其设置为环境变量（`HUGGINGFACE_API_KEY`）

## 包装器

### LLM

存在一个Petals LLM包装器，您可以使用以下代码访问：

```python
from langchain_community.llms import Petals
```

[20]