# 2Markdown

[2markdown](https://2markdown.com/) 服务可以将网站内容转换为结构化的 Markdown 文件。

## 安装和设置

我们需要使用 `API key`。请参阅[获取 API key 的说明](https://2markdown.com/login)。

## 文档加载器

查看一个[使用示例](/docs/integrations/document_loaders/tomarkdown)。

```python
from langchain_community.document_loaders import ToMarkdownLoader
```