# 随机AI

本页面介绍如何在 LangChain 中使用随机AI生态系统。

分为两部分：安装和设置，以及特定随机AI包装器的参考。

## 安装和设置

- 使用 `pip install stochasticx` 进行安装

- 获取一个随机AI API密钥，并将其设置为环境变量 (`STOCHASTICAI_API_KEY`)

## 包装器

### LLM

存在一个随机AI LLM包装器，您可以使用以下代码访问：

```python
from langchain_community.llms import StochasticAI
```