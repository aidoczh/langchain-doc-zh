# 写作工具

本页面介绍如何在 LangChain 中使用写作工具生态系统。

内容分为两部分：安装和设置，以及特定写作工具包的引用。

## 安装和设置

- 获取一个写作工具的 API 密钥，并将其设置为环境变量 (`WRITER_API_KEY`)

## 工具包

### LLM

LangChain 提供了一个写作工具 LLM 的包装器，您可以通过以下方式访问：

```python
from langchain_community.llms import Writer
```