# DuckDB

>[DuckDB](https://duckdb.org/) 是一种嵌入式 SQL OLAP 数据库管理系统。

## 安装和设置

首先，您需要安装 `duckdb` Python 包。

```bash
pip install duckdb
```

## 文档加载器

请参阅 [使用示例](/docs/integrations/document_loaders/duckdb)。

```python
from langchain_community.document_loaders import DuckDBLoader
```