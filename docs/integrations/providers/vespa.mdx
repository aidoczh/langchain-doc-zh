# Vespa

[Vespa](https://vespa.ai/) 是一个功能齐全的搜索引擎和向量数据库。它支持向量搜索（ANN）、词汇搜索以及在同一查询中搜索结构化数据。

## 安装和设置

```bash
pip install pyvespa
```

## Retriever

查看一个[使用示例](/docs/integrations/retrievers/vespa)。

```python
from langchain.retrievers import VespaRetriever
```