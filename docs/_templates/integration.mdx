[comment: 请在这里引用一个参考文献示例 "docs/integrations/arxiv.md"]::

[comment: 使用此模板在 "docs/integrations/" 目录下创建一个新的 .md 文件]::

# 标题_REPLACE_ME

[comment: 只允许一个标题/H1!]::

>

[comment: 描述: 阅读完这段描述后，读者应该能决定是否足够好以至于值得尝试/继续阅读，或者]::

[comment: 前往阅读下一个集成文档。]::

[comment: 描述应包括一个指向源文档的链接以供进一步阅读。]::

## 安装与设置

[comment: 安装与设置: 所有必要的额外软件包安装和 Tokens 等的设置]::

```bash
pip install package_name_REPLACE_ME
```

[comment: 或者这段文字:]::

对此没有特殊设置。

[comment: 下一个 H2/## 部分包括集成模块的名称，如 "LLM"，"文本嵌入模型" 等]::

[comment: 请参见 "index.html" 页面中的 "Modules"]::

[comment: 每个 H2 部分应包括一个示例链接和一个导入集成类的 Python 代码]::

[comment: 以下是几个示例部分。删除所有不必要的部分。添加这里未提供的所有必要部分。]::

## LLM

查看一个[使用示例](/docs/integrations/llms/INCLUDE_REAL_NAME)。

```python
from langchain_community.llms import integration_class_REPLACE_ME
```

## 文本嵌入模型

查看一个[使用示例](/docs/integrations/text_embedding/INCLUDE_REAL_NAME)。

```python
from langchain_community.embeddings import integration_class_REPLACE_ME
```

## 聊天模型

查看一个[使用示例](/docs/integrations/chat/INCLUDE_REAL_NAME)。

```python
from langchain_community.chat_models import integration_class_REPLACE_ME
```

## 文档加载器

查看一个[使用示例](/docs/integrations/document_loaders/INCLUDE_REAL_NAME)。

```python
from langchain_community.document_loaders import integration_class_REPLACE_ME
```